# Block direct access to toolkit files
# Only allow access through secure download API

# Deny all direct access
Order Deny,Allow
Deny from all

# Optionally allow specific IPs (your server)
# Allow from 127.0.0.1

# Block specific file types
<FilesMatch "\.(pdf|zip|doc|docx)$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

# Custom error page for blocked access
ErrorDocument 403 "Access denied. Please use the download links from your purchase confirmation."
ErrorDocument 404 "File not found. Please check your purchase confirmation email."