# Block direct access to PDF and ZIP files
# Only allow access through secure API

Order Deny,Allow
Deny from all

# Block all file types in this directory
<FilesMatch ".*">
    Order Deny,Allow  
    Deny from all
</FilesMatch>